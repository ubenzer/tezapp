"use strict"
ngDefine "controllers.results.entityDetail", (module) ->
  module.controller "results.entityDetail", ($scope, $http, SelectedItems, $filter) ->
      console.debug($scope)
      return
  return